# Example rules only — replace with YOUR instance's market/language vocabulary. See instances/_TEMPLATE/INSTANCE.md.
#
# GENERATED by scripts/seed_operators.py — DO NOT EDIT BY HAND
# Source: instances/_TEMPLATE/data/operators.yaml
#
# Each pattern captures any surface form of an operator's name and
# force-normalizes to the canonical brand entity. Loaded by
# processors/rules_layer.py alongside identifiers.yaml.
#
# Entries below are GENERIC PLACEHOLDER examples demonstrating the structure:
# a regex matching one-or-more surface forms, collapsed via `force:<canonical>`.
# Populate per-instance from your operators.yaml seed file.

brand:
  # examplebet — single canonical name with several surface variants
  - regex: '\b(examplebet)(?:bet|local|\d|\.(?:com|online))?\w*\b'
    kind: brand
    normalize: ['force:examplebet']
  # slotbrand-a
  - regex: '\b(slotbrand-?a)\b'
    kind: brand
    normalize: ['force:slotbrand-a']
  # betbrand-b — demonstrates collapsing two surface forms to one entity
  - regex: '\b(betbrand-?b|betbrandb)\b'
    kind: brand
    normalize: ['force:betbrand-b']
